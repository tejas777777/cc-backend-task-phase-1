# cc-backend-task-phase3
nginx has been configured and the details are in the config file.
have switched from sqlite3 to postgresql
race condition, idempotency condition and atomic operations have been handled in the checkout operation
now using an ubuntu vm in azure, having a public ip address
self signed certificate implemented
docker image has been built and executed
domain name applied for and which will soon be available - https://www.thinkit.codes
pagination has been implemented globally
